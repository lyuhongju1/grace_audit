Stage outputs from the first bundle (paper run of 2026-08-11), kept for
comparison. Differences from the current *_full.txt are explained in the
top-level README, section 7: the accounting table changes because the
mechanism search no longer lets a JSON-leaf target feed its own pair
formulas (357 -> 448 diagnosed, 1487 -> 1396 unexplained), and because the
grammar gained a*(1+b/100). Re-execution and invariant outputs are unchanged.
